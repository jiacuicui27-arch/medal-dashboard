# 勋章看板定时更新配置说明

## 已创建的文件

| 文件 | 说明 |
|------|------|
| `build_dashboard.py` | 数据构建脚本，读取CSV和Excel → 生成新HTML |
| `update_dashboard.bat` | Windows批处理脚本，用于Windows任务计划 |

## 方案一：Windows 任务计划程序（推荐 ✅）

最可靠，Windows 开机就能触发，不依赖 WSL 是否运行。

### 步骤

1. 按 `Win + R`，输入 `taskschd.msc`，打开"任务计划程序"
2. 点击右侧"创建基本任务"
3. 名称：`勋章看板每日更新`
4. 触发器：每天，时间 10:00
5. 操作：启动程序
   - 程序：`D:\CC勋章看板\update_dashboard.bat`
6. 完成

### 或者用命令行一步到位（以管理员身份运行 PowerShell）：

```powershell
schtasks /create /tn "勋章看板每日更新" /tr "D:\CC勋章看板\update_dashboard.bat" /sc daily /st 10:00 /f
```

## 方案二：WSL crontab

在 WSL 终端中执行：

```bash
# 编辑 crontab
crontab -e

# 添加以下行（每天10点运行）
0 10 * * * /usr/bin/python3 /mnt/d/CC勋章看板/build_dashboard.py >> /mnt/d/CC勋章看板/update_log.txt 2>&1
```

> ⚠️ 注意：WSL crontab 需要 WSL 服务在运行。如果 Windows 重启后没打开过 WSL，任务不会执行。

## 方案三：手动运行

随时在 WSL 终端执行：

```bash
python3 /mnt/d/CC勋章看板/build_dashboard.py
```

## 更新日志

脚本执行后会自动在 `update_log.txt` 中记录运行结果。

## 数据源

| 文件 | 说明 | 更新时间 |
|------|------|----------|
| `勋章获得数据.csv` | 用户勋章获得数据 | 每天9点自动更新 |
| `勋章映射.xlsx` | 勋章code→名称映射 | 不常更新 |
| `勋章获得条件及上线日期.xlsx` | 勋章元数据 | 新勋章上线时更新 |
